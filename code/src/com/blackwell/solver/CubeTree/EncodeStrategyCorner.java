package com.blackwell.solver.CubeTree;

import com.blackwell.solver.cube.Cube;

public class EncodeStrategyCorner implements EncodeStrategy {
    @Override
    public int doEncode(Cube cube) {
        final int CORNERS = 8;
        int[] shiftFactors = new int[CORNERS];
        for(int i=0; i<CORNERS; ++i){

            // TODO make it encode here

        }
        return 0;
    }
}
